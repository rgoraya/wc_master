$("#issues").html("<%= escape_javascript(render("issues")) %>");
$(".formcontentdiv").effect("highlight", {color: '#f5f5f5'}, 1500);

if ($.trim($('#notice').text()) != "") {
	$("#notice_container").effect("highlight", {color: '#4DB8DB'}, 800);
  	$("#notice").show;
  }

	setTimeout(function() {
	    $('#notice_container').fadeOut('slow');
	    $('#notice').empty();	
	}, 3000);